{
  "scripts": {
    "test": "jest --verbose --silent",
    "test:watch": "jest --verbose --watchAll --silent",
    "coverage": "jest --coverage",
    "dev": "nodemon src/index.js"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "jest": "^28.1.1",
    "nodemon": "^2.0.16",
    "supertest": "^6.2.3"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
